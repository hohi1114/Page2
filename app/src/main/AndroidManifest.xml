<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.page2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SubActivity_01"
            android:label="SubActivity_01" />
        <activity android:name=".SubActivity_02"></activity>

        <activity android:name=".MusicItemClicked"></activity>

        <service android:name=".MusicService"/>
        <service android:name=".MusicService1"/>
        <service android:name=".MusicService2"/>
        <service android:name=".MusicService3"/>


    </application>

</manifest>